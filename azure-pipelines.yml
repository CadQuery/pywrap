trigger:
  branches:
    include:
      - master
      - refs/tags/*

pr: none

jobs:
- template: build-bindings-job.yml
  parameters:
    name: Linux
- template: build-bindings-job.yml
  parameters:
    name: Windows
    vmImage: 'vs2017-win2016'